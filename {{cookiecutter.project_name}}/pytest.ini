[pytest]
testpaths = tests/

addopts =
    --strict

    --verbose --verbose
    -r sxX

    --cov={{cookiecutter.project_name}}
    --cov-report=html
    --cov-report=term-missing:skip-covered
    --no-cov-on-fail

cache_dir = .cache

markers =
